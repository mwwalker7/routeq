{
  "name": "routeq",
  "version": "0.1.0",
  "description": "Query which route in a routing table matches a URL, one line of input at a time",
  "license": "MIT",
  "type": "module",
  "bin": {
    "routeq": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
